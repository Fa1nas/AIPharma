@model List<AIPharma.Models.Complaint>

@{
    ViewData["Title"] = "Звернення аптеки";
}

<section class="page-header">
    <h1>Звернення по моїй аптеці</h1>
    <p>Менеджер бачить тільки звернення, які стосуються його аптеки.</p>
</section>

@if (!Model.Any())
{
    <div class="note-section">
        <p>Звернень по цій аптеці поки що немає.</p>
    </div>
}
else
{
    <table class="data-table">
        <thead>
            <tr>
                <th>№</th>
                <th>Користувач</th>
                <th>Тема</th>
                <th>Статус</th>
                <th>Дата</th>
                <th>Дії</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var complaint in Model)
            {
                <tr>
                    <td>@complaint.Id</td>
                    <td>@complaint.User?.FullName</td>
                    <td>@complaint.Title</td>
                    <td>@complaint.Status</td>
                    <td>@complaint.CreatedAt.ToString("dd.MM.yyyy HH:mm")</td>
                    <td>
                        <a class="btn-small" asp-controller="Manager" asp-action="ComplaintDetails" asp-route-id="@complaint.Id">
                            Деталі
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}